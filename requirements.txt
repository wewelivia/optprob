# Core quant + API
numpy>=1.24
scipy>=1.10
pandas>=2.0
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0
python-multipart>=0.0.9
jinja2>=3.1

# Bloomberg (install on the Terminal machine only)
# xbbg>=0.7.7
# blpapi>=3.24.6   # from the Bloomberg package index:
#   pip install --index-url=https://blpapi.bloomberg.com/repository/releases/python/simple/ blpapi

# Notebook / validation (optional)
matplotlib>=3.7
notebook>=7.0
nbconvert>=7.0
ipykernel>=6.0
